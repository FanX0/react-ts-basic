## Production env
# Read-only demo mode (uses public/data.json). set VITE_USE_STATIC_DATA=true to disbale mutations(demo mode)
# set VITE_USE_STATIC_DATA=false to enable mutations.
# have a backend API available in production.
VITE_USE_STATIC_DATA=false

# To enable full CRUD in production:
# 1) Deploy an API (JSON Server/Express/etc.) reachable from your site.
# 2) Set the public base URL below and switch static mode off.
# 3) Rebuild and redeploy.
#
# Example:
# VITE_USE_STATIC_DATA=false
# VITE_API_BASE_URL=https://your-api.example.com