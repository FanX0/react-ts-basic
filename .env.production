## Production env
#setting VITE_USE_STATIC_DATA
    #true → Demo/Read-only mode
        #Data comes from public/*.json
        #No create/update/delete (CRUD disabled)
        #Works offline, no backend required
        #Perfect for previews, design QA, static hosting, Netlify/Vercel previews
    #false → Live/CRUD mode
        #Data comes from your backend API
        #CRUD enabled
        #Requires VITE_API_BASE_URL to be set and CORS configured
        #Use for staging/production when your API is available
VITE_USE_STATIC_DATA=true

# To enable full CRUD in production:
# 1) Deploy an API (JSON Server/Express/etc.) reachable from your site.
# 2) Set the public base URL below and switch static mode off.
# 3) Rebuild and redeploy.
#
# Example:
# VITE_USE_STATIC_DATA=false
# VITE_API_BASE_URL=https://your-api.example.com